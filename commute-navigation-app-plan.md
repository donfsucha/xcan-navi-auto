# 출퇴근 음악/내비 자동 실행 앱 개발 계획서

## 1. 앱 목표

이 앱은 사용자가 등록한 집과 회사 주소, 출근/퇴근 시간, 선택한 음악 앱, 선택한 내비 앱을 기준으로 출퇴근 상황에 맞는 실행 흐름을 자동화한다.

기본 실행 순서는 다음과 같다.

1. 선택한 음악 앱 실행
2. Android 미디어 재생 버튼 이벤트 전송
3. 선택한 모바일 내비 앱 실행

평일 출근 시간에는 회사로 길안내를 실행하고, 평일 퇴근 시간에는 집으로 길안내를 실행한다. 주말모드에서는 출근/퇴근 목적지 판단을 생략하고 음악 실행 후 선택한 모바일 내비 앱만 실행한다.

## 2. 핵심 요구사항

### 2.1 주소 설정

- 사용자는 집 주소를 등록할 수 있어야 한다.
- 사용자는 회사 주소를 등록할 수 있어야 한다.
- 초기 버전에서는 주소 문자열을 직접 입력한다.
- 이후 버전에서 주소 검색 및 좌표 변환을 추가한다.

### 2.2 내비 앱 선택

설정 화면에서 사용할 내비 앱을 선택할 수 있어야 한다.

- 티맵
- 카카오내비
- 네이버지도

선택한 내비 앱이 설치되어 있지 않으면 사용자에게 설치 안내를 제공한다.

### 2.3 음악 앱 선택

설정 화면에서 사용할 음악 앱을 선택할 수 있어야 한다.

- 삼성뮤직
- 멜론
- 유튜브 뮤직
- 스포티파이

선택한 음악 앱이 설치되어 있지 않으면 사용자에게 설치 안내를 제공한다.

### 2.4 음악 자동 재생 시도

1차 버전부터 단순히 음악 앱을 여는 것에 그치지 않고, 음악 앱 실행 후 Android 미디어 버튼 이벤트를 전송하여 자동 재생을 시도한다.

실행 흐름은 다음과 같다.

1. 선택한 음악 앱 실행
2. 1~2초 대기
3. `MEDIA_PLAY` 이벤트 전송
4. 짧게 대기
5. 선택한 내비 앱 실행

주의할 점은 다음과 같다.

- 이 기능은 블루투스 재생 버튼을 누른 것과 비슷한 방식으로 동작한다.
- Android OS 버전, 음악 앱 정책, 로그인 상태, 마지막 재생 상태, 차량 블루투스 연결 상태에 따라 실패할 수 있다.
- 따라서 제품 문구와 내부 구현에서는 "자동 재생 보장"이 아니라 "자동 재생 시도"로 정의한다.
- `MEDIA_PLAY_PAUSE`는 이미 음악이 재생 중일 때 음악을 멈출 수 있으므로 기본값은 `MEDIA_PLAY`로 한다.

### 2.5 주말모드

주말모드가 켜져 있고 현재 날짜가 토요일 또는 일요일이면 출근/퇴근 판단을 건너뛴다.

주말모드 실행 흐름은 다음과 같다.

1. 선택한 음악 앱 실행
2. 미디어 재생 버튼 이벤트 전송
3. 선택한 모바일 내비 앱 실행

주말모드에서는 기본적으로 목적지를 지정하지 않고 내비 앱만 실행한다. 단, 메인 화면에서 집 또는 회사를 수동 목적지로 선택할 수 있도록 확장할 수 있다.

## 3. 화면 구성

### 3.1 첫 실행 설정 화면

앱을 처음 실행했을 때 필수 설정을 입력받는다.

- 집 주소 입력
- 회사 주소 입력
- 출근 시간대 설정
- 퇴근 시간대 설정
- 기본 내비 앱 선택
- 기본 음악 앱 선택
- 음악 자동 재생 시도 여부
- 주말모드 사용 여부

### 3.2 메인 화면

메인 화면은 현재 상태를 한눈에 보여주고 바로 실행할 수 있어야 한다.

표시 항목:

- 현재 판단된 모드
  - 출근 모드
  - 퇴근 모드
  - 주말 모드
  - 수동 실행 모드
- 선택된 음악 앱
- 선택된 내비 앱
- 목적지
  - 회사
  - 집
  - 없음
- 실행 버튼

주요 버튼:

- 음악 + 내비 실행
- 회사로 가기
- 집으로 가기
- 설정

### 3.3 설정 화면

사용자가 언제든지 실행 방식을 바꿀 수 있어야 한다.

- 집 주소 수정
- 회사 주소 수정
- 출근 시간대 수정
- 퇴근 시간대 수정
- 내비 앱 변경
- 음악 앱 변경
- 음악 자동 재생 시도 ON/OFF
- 음악 앱 실행 후 대기 시간 설정
- 미디어 버튼 방식 선택
  - 재생
  - 재생/일시정지
- 주말모드 ON/OFF

## 4. 데이터 모델

```kotlin
data class UserSettings(
    val homePlace: Place,
    val workPlace: Place,
    val commuteStartTimeRange: TimeRange,
    val commuteEndTimeRange: TimeRange,
    val selectedNavigationApp: NavigationApp,
    val selectedMusicApp: MusicApp,
    val autoPlayMusicEnabled: Boolean,
    val mediaCommandType: MediaCommandType,
    val musicLaunchDelayMs: Long,
    val weekendModeEnabled: Boolean
)

data class Place(
    val name: String,
    val address: String,
    val latitude: Double? = null,
    val longitude: Double? = null
)

data class TimeRange(
    val startHour: Int,
    val startMinute: Int,
    val endHour: Int,
    val endMinute: Int
)

enum class NavigationApp {
    TMAP,
    KAKAO_NAVI,
    NAVER_MAP
}

enum class MusicApp {
    SAMSUNG_MUSIC,
    MELON,
    YOUTUBE_MUSIC,
    SPOTIFY
}

enum class MediaCommandType {
    PLAY,
    PLAY_PAUSE
}

enum class DriveMode {
    GO_TO_WORK,
    GO_HOME,
    WEEKEND,
    MANUAL
}
```

## 5. 전체 앱 구조

Android 앱 기준으로 다음 구조를 권장한다.

```text
app
 +-- ui
 |   +-- MainScreen
 |   +-- SettingsScreen
 |   +-- AddressEditScreen
 |   +-- AppPickerScreen
 |
 +-- domain
 |   +-- DriveModeResolver
 |   +-- CommuteFlowUseCase
 |   +-- LaunchMusicUseCase
 |   +-- LaunchNavigationUseCase
 |
 +-- data
 |   +-- UserSettingsRepository
 |   +-- SettingsDataStore
 |   +-- PlaceRepository
 |
 +-- integration
 |   +-- MusicLauncher
 |   +-- MediaCommandDispatcher
 |   +-- NavigationLauncher
 |   +-- AppInstallChecker
 |   +-- IntentFactory
 |
 +-- model
     +-- UserSettings
     +-- Place
     +-- TimeRange
     +-- NavigationApp
     +-- MusicApp
     +-- MediaCommandType
     +-- DriveMode
```

## 6. 주요 컴포넌트 역할

### 6.1 DriveModeResolver

현재 날짜, 현재 시간, 사용자 설정값을 기준으로 실행 모드를 판단한다.

판단 기준:

- 토요일/일요일이고 주말모드 ON이면 `WEEKEND`
- 평일이고 출근 시간대이면 `GO_TO_WORK`
- 평일이고 퇴근 시간대이면 `GO_HOME`
- 그 외에는 `MANUAL`

### 6.2 CommuteFlowUseCase

앱의 전체 실행 순서를 담당한다.

기본 흐름:

1. 설정값 읽기
2. 현재 모드 판단
3. 목적지 결정
4. 음악 앱 실행
5. 음악 자동 재생 시도
6. 내비 앱 실행
7. 실패 상황 처리

### 6.3 MusicLauncher

선택한 음악 앱을 실행한다.

담당 범위:

- 선택한 음악 앱의 패키지명 확인
- 앱 설치 여부 확인
- 앱 실행 Intent 생성
- 실행 실패 처리

### 6.4 MediaCommandDispatcher

음악 앱 실행 후 미디어 버튼 이벤트를 전송한다.

기본 구현 방향:

```kotlin
fun sendMediaPlayButton(context: Context) {
    val audioManager = context.getSystemService(Context.AUDIO_SERVICE) as AudioManager

    audioManager.dispatchMediaKeyEvent(
        KeyEvent(KeyEvent.ACTION_DOWN, KeyEvent.KEYCODE_MEDIA_PLAY)
    )
    audioManager.dispatchMediaKeyEvent(
        KeyEvent(KeyEvent.ACTION_UP, KeyEvent.KEYCODE_MEDIA_PLAY)
    )
}
```

설정에서 `PLAY_PAUSE`를 선택한 경우 다음 키 코드를 사용할 수 있다.

```kotlin
KeyEvent.KEYCODE_MEDIA_PLAY_PAUSE
```

기본값은 `KEYCODE_MEDIA_PLAY`를 사용한다.

### 6.5 NavigationLauncher

선택한 내비 앱을 실행하고 목적지를 전달한다.

담당 범위:

- 티맵 실행 Intent 생성
- 카카오내비 실행 Intent 생성
- 네이버지도 실행 Intent 생성
- 주소 또는 좌표 기반 목적지 전달
- 앱 미설치 시 설치 안내
- 실행 실패 시 fallback 처리

### 6.6 AppInstallChecker

선택한 외부 앱이 설치되어 있는지 확인한다.

확인 대상:

- 음악 앱
- 내비 앱

앱이 설치되어 있지 않으면 다음 중 하나를 제공한다.

- 설치 안내 메시지
- Play Store 이동
- 다른 앱 선택 유도

## 7. 실행 시나리오

### 7.1 평일 출근 시간

조건:

- 오늘이 월요일~금요일
- 현재 시간이 출근 시간대
- 회사 주소가 등록되어 있음

흐름:

1. 선택한 음악 앱 실행
2. 설정된 대기 시간만큼 대기
3. 미디어 재생 버튼 이벤트 전송
4. 회사 주소를 목적지로 선택
5. 선택한 내비 앱으로 회사 길안내 실행

### 7.2 평일 퇴근 시간

조건:

- 오늘이 월요일~금요일
- 현재 시간이 퇴근 시간대
- 집 주소가 등록되어 있음

흐름:

1. 선택한 음악 앱 실행
2. 설정된 대기 시간만큼 대기
3. 미디어 재생 버튼 이벤트 전송
4. 집 주소를 목적지로 선택
5. 선택한 내비 앱으로 집 길안내 실행

### 7.3 주말모드

조건:

- 오늘이 토요일 또는 일요일
- 주말모드가 켜져 있음

흐름:

1. 선택한 음악 앱 실행
2. 설정된 대기 시간만큼 대기
3. 미디어 재생 버튼 이벤트 전송
4. 선택한 내비 앱 실행

### 7.4 수동 실행 모드

조건:

- 출근/퇴근 시간대가 아님
- 주말모드 조건도 아님

흐름:

1. 사용자에게 목적지를 선택하게 한다.
2. 회사 또는 집 선택 시 음악 실행 후 해당 목적지로 내비를 실행한다.
3. 목적지 없이 실행을 선택하면 음악 실행 후 내비 앱만 실행한다.

## 8. 1차 MVP 개발 범위

1차 버전에서 구현할 기능은 다음과 같다.

- Android 앱 기본 프로젝트 구성
- 집 주소 저장
- 회사 주소 저장
- 출근 시간대 저장
- 퇴근 시간대 저장
- 내비 앱 선택
- 음악 앱 선택
- 주말모드 설정
- 음악 자동 재생 시도 설정
- 음악 앱 실행
- 미디어 재생 버튼 이벤트 전송
- 내비 앱 실행
- 평일 출근/퇴근 목적지 자동 판단
- 기본 예외 처리

1차 버전에서 제외할 기능은 다음과 같다.

- 주소 검색
- 주소 좌표 변환
- 음악 앱별 정밀 자동 재생 API 검토
- 차량 블루투스 연결 감지 자동 실행
- Android Auto 연동
- 백그라운드 완전 자동 실행

## 9. 단계별 개발 계획

### 9.1 1단계: 프로젝트 생성

- Android Kotlin 프로젝트 생성
- Jetpack Compose 사용 여부 결정
- 최소 SDK 버전 결정
- 패키지명 결정
- 기본 화면 실행 확인

완료 기준:

- 앱이 빌드되고 메인 화면이 표시된다.

### 9.2 2단계: 설정 저장 기능

- `UserSettings` 모델 작성
- `Place`, `TimeRange`, `NavigationApp`, `MusicApp`, `MediaCommandType`, `DriveMode` 모델 작성
- DataStore 기반 설정 저장소 구현
- 설정 읽기/쓰기 기능 구현

완료 기준:

- 앱을 종료했다가 다시 실행해도 설정값이 유지된다.

### 9.3 3단계: 설정 UI

- 집 주소 입력 UI
- 회사 주소 입력 UI
- 출근 시간대 선택 UI
- 퇴근 시간대 선택 UI
- 내비 앱 선택 UI
- 음악 앱 선택 UI
- 음악 자동 재생 시도 ON/OFF UI
- 음악 앱 실행 후 대기 시간 UI
- 주말모드 ON/OFF UI

완료 기준:

- 사용자가 앱 안에서 모든 필수 설정을 입력하고 저장할 수 있다.

### 9.4 4단계: 앱 설치 여부 확인

- 음악 앱 패키지명 정의
- 내비 앱 패키지명 정의
- 설치 여부 확인 로직 구현
- 미설치 앱 안내 UI 구현

완료 기준:

- 선택한 음악 앱과 내비 앱의 설치 여부를 확인할 수 있다.

### 9.5 5단계: 음악 앱 실행

- `MusicLauncher` 구현
- 선택한 음악 앱 실행 Intent 생성
- 앱 실행 실패 예외 처리

완료 기준:

- 선택한 음악 앱이 실행된다.

### 9.6 6단계: 음악 자동 재생 시도

- `MediaCommandDispatcher` 구현
- `KEYCODE_MEDIA_PLAY` 이벤트 전송
- 설정에 따라 `KEYCODE_MEDIA_PLAY_PAUSE` 이벤트 전송
- 음악 앱 실행 후 지연 시간 적용
- 실패하거나 반응이 없을 때 안내 처리

완료 기준:

- 음악 앱 실행 후 미디어 재생 버튼 이벤트가 전송된다.

### 9.7 7단계: 내비 앱 실행

- `NavigationLauncher` 구현
- 티맵 실행 구현
- 카카오내비 실행 구현
- 네이버지도 실행 구현
- 목적지 주소 전달 방식 구현
- 목적지 없이 내비 앱만 여는 방식 구현

완료 기준:

- 선택한 내비 앱이 실행되고, 출근/퇴근 모드에서는 올바른 목적지가 전달된다.

### 9.8 8단계: 출근/퇴근 판단 로직

- `DriveModeResolver` 구현
- 평일/주말 판단
- 출근 시간대 판단
- 퇴근 시간대 판단
- 목적지 결정 로직 구현

완료 기준:

- 출근 시간에는 회사, 퇴근 시간에는 집이 자동으로 선택된다.

### 9.9 9단계: 전체 실행 흐름 연결

- `CommuteFlowUseCase` 구현
- 음악 앱 실행
- 미디어 재생 버튼 이벤트 전송
- 내비 앱 실행
- 단계별 실패 처리

완료 기준:

- 메인 화면의 실행 버튼을 누르면 음악 앱 실행, 자동 재생 시도, 내비 앱 실행이 순서대로 동작한다.

### 9.10 10단계: 주말모드 구현

- 토요일/일요일 판단
- 주말모드 ON일 때 출근/퇴근 목적지 판단 생략
- 음악 실행 후 내비 앱만 실행

완료 기준:

- 주말모드에서는 목적지 없이 음악과 내비 앱만 순서대로 실행된다.

### 9.11 11단계: 예외 처리 및 안정화

- 집 주소 미입력 처리
- 회사 주소 미입력 처리
- 음악 앱 미설치 처리
- 내비 앱 미설치 처리
- 음악 앱 실행 실패 처리
- 내비 앱 실행 실패 처리
- 출근/퇴근 시간대 중복 처리
- Android 버전별 동작 차이 확인

완료 기준:

- 실패 상황에서도 앱이 종료되지 않고 사용자에게 명확한 안내를 제공한다.

### 9.12 12단계: 실제 기기 테스트

테스트 항목:

- 삼성뮤직 실행
- 멜론 실행
- 유튜브 뮤직 실행
- 스포티파이 실행
- 음악 자동 재생 시도
- 티맵 길안내
- 카카오내비 길안내
- 네이버지도 길안내
- 평일 출근 시간 테스트
- 평일 퇴근 시간 테스트
- 주말모드 테스트
- 앱 미설치 상태 테스트
- 블루투스 차량 연결 상태 테스트

완료 기준:

- 주요 시나리오가 실제 Android 기기에서 검증된다.

## 10. 권장 구현 우선순위

개발 우선순위는 다음과 같다.

1. 설정 저장
2. 메인 화면
3. 음악 앱 실행
4. 미디어 재생 버튼 이벤트 전송
5. 내비 앱 실행
6. 출근/퇴근 판단
7. 주말모드
8. 예외 처리
9. 실제 기기 테스트
10. 주소 검색 및 좌표 변환
11. 차량 블루투스 연결 감지
12. Android Auto 연동 검토

## 11. 주요 기술 리스크

### 11.1 음악 자동 재생 성공률

미디어 버튼 이벤트는 Android에서 지원되는 방식이지만, 모든 음악 앱이 항상 같은 방식으로 반응하지는 않는다. 따라서 자동 재생은 보장 기능이 아니라 시도 기능으로 정의해야 한다.

대응:

- 기본값은 `MEDIA_PLAY` 사용
- 설정에서 `MEDIA_PLAY_PAUSE` 선택 가능
- 앱 실행 후 대기 시간 조절 가능
- 앱별 테스트 결과를 기록하여 추후 보정

### 11.2 내비 앱 목적지 전달 방식 차이

티맵, 카카오내비, 네이버지도는 목적지 전달 방식과 URL Scheme이 다를 수 있다.

대응:

- 앱별 `IntentFactory` 분리
- 주소 문자열 기반 실행을 우선 구현
- 이후 좌표 기반 실행으로 정확도 개선
- 앱 실행 실패 시 fallback 제공

### 11.3 Android 백그라운드 실행 제한

Android는 백그라운드에서 다른 앱을 자동 실행하는 동작을 제한할 수 있다.

대응:

- 1차 버전은 사용자가 앱의 실행 버튼을 누르는 방식으로 구현
- 이후 알림, 위젯, 차량 블루투스 연결 감지 등으로 확장

## 12. 2차 버전 확장 계획

2차 버전에서는 다음 기능을 검토한다.

- 주소 검색 기능
- 주소를 위도/경도로 변환
- 내비 앱별 더 정확한 길안내 URL 적용
- 음악 앱별 자동 재생 성공률 개선
- 차량 블루투스 연결 시 앱 실행 제안 알림
- 홈 화면 위젯
- 빠른 실행 타일
- Android Auto 연동 가능성 검토

## 13. 최종 MVP 정의

최초 출시 가능한 MVP는 다음 조건을 만족해야 한다.

- 사용자가 집과 회사 주소를 저장할 수 있다.
- 사용자가 출근/퇴근 시간대를 설정할 수 있다.
- 사용자가 음악 앱을 선택할 수 있다.
- 사용자가 내비 앱을 선택할 수 있다.
- 실행 버튼을 누르면 음악 앱이 먼저 열린다.
- 음악 앱 실행 후 미디어 재생 버튼 이벤트를 전송한다.
- 이후 선택한 내비 앱이 열린다.
- 출근 시간에는 회사로 안내한다.
- 퇴근 시간에는 집으로 안내한다.
- 주말모드에서는 목적지 판단 없이 음악 실행 후 내비 앱만 실행한다.
- 앱이 설치되어 있지 않거나 설정값이 부족할 때 명확한 안내를 제공한다.
