# API 키 입력 전 준비 상태

이 프로젝트는 실제 API 키를 코드에 직접 넣지 않고 `local.properties`에서 읽도록 준비되어 있습니다.

## 입력 위치

`local.properties`에 아래 값을 추가합니다.

```properties
KAKAO_NATIVE_APP_KEY=
NAVER_MAP_CLIENT_ID=
TMAP_APP_KEY=
GOOGLE_MAPS_API_KEY=
```

실제 키는 채워 넣되, `local.properties`는 커밋하지 않습니다.

기존 프로젝트에 키가 들어 있더라도 새 프로젝트 코드에 직접 복사하지 않습니다. 키는 로컬의 `local.properties`에만 직접 입력하세요.

## 앱 안 표시

앱의 `탐색` 탭에서 각 키의 준비 상태를 확인할 수 있습니다.

- `입력 필요`: 아직 빈 값
- `연결 준비됨`: `local.properties`에 값이 있음

## API 키 입력 후 다음 작업

키 입력 후 진행할 작업은 다음과 같습니다.

1. 앱 내부 지도 SDK 연결
2. 집/회사 선택 모드 추가
3. 지도 터치 좌표 자동 입력
4. 선택 좌표 저장
5. 저장 좌표 기반 네이버지도/카카오내비/티맵 길안내 실행 검증

## 현재 상태

현재는 API 키 없이도 다음 기능이 동작합니다.

- 집/회사 주소 입력
- 집/회사 위도/경도 입력
- 지도 앱으로 좌표 확인
- 좌표 확인 완료
- 저장 좌표 기반 길안내 실행
- 홈/설정/탐색 탭 UI
- Google Maps 기반 지도 터치 좌표 자동 입력

카카오 키를 입력하면 추가로 다음 기능이 동작합니다.

- 카카오내비 선택 시 Kakao Navi SDK 초기화
- 저장된 집/회사 좌표를 `CoordType.WGS84`로 카카오내비에 전달
- 카카오내비 미설치 시 설치 화면 안내
